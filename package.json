{
  "scripts": {
    "start": "npm run --prefix src start",
    "android": "npm run --prefix src android",
    "ios": "npm run --prefix src ios",
    "ios:localhost": "npm run --prefix src ios:localhost",
    "web": "npm run --prefix src web",
    "test": "npm run --prefix src test"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.50.0",
    "@twodoorsdev/react-native-debugger-mcp": "^1.0.0",
    "cali-mcp-server": "^0.3.1"
  }
}
